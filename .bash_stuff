# amazon aws
if [[ -f "$HOME/.amazon_keys" ]]; then
  source "$HOME/.amazon_keys";
fi

# hadoop
if [[ -f "$HOME/.hadoop" ]]; then
  source "$HOME/.hadoop";
fi

# EC2
if [[ -f "$HOME/.ec2" ]]; then
  source "$HOME/.ec2";
fi
